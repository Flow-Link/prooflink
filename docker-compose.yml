services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: flowlink
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-flowlink_dev}
      POSTGRES_DB: flowlink
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flowlink"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - flowlink

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - flowlink

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    restart: unless-stopped
    ports:
      - "${API_PORT:-3001}:3001"
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql://flowlink:${POSTGRES_PASSWORD:-flowlink_dev}@postgres:5432/flowlink
      REDIS_URL: redis://redis:6379
      NODE_ENV: production
      PORT: "3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
    networks:
      - flowlink

  api-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    command: ["pnpm", "--filter=@flowlink/api", "dev"]
    ports:
      - "${API_PORT:-3001}:3001"
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql://flowlink:${POSTGRES_PASSWORD:-flowlink_dev}@postgres:5432/flowlink
      REDIS_URL: redis://redis:6379
      NODE_ENV: development
    volumes:
      - ./packages/shared/src:/app/packages/shared/src
      - ./packages/core/src:/app/packages/core/src
      - ./packages/sdk/src:/app/packages/sdk/src
      - ./apps/api/src:/app/apps/api/src
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - flowlink

  dashboard:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    command: ["pnpm", "--filter=@flowlink/dashboard", "dev"]
    ports:
      - "${DASHBOARD_PORT:-3100}:3100"
    volumes:
      - ./apps/dashboard/src:/app/apps/dashboard/src
      - ./apps/dashboard/public:/app/apps/dashboard/public
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:${API_PORT:-3001}
    depends_on:
      - api-dev
    networks:
      - flowlink

volumes:
  postgres_data:
  redis_data:

networks:
  flowlink:
    driver: bridge
